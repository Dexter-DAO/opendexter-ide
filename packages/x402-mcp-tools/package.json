{
  "name": "@dexterai/x402-mcp-tools",
  "version": "0.9.1",
  "description": "Shared MCP tool registrations for the Dexter x402 ecosystem. One register*Tool function per tool, plus widget metadata. Consumed by @dexterai/opendexter (npm CLI), the hosted public MCP server, and the hosted authenticated MCP server.",
  "type": "module",
  "main": "dist/index.cjs",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "require": "./dist/index.cjs"
    }
  },
  "files": [
    "dist",
    "PURCHASE-CONTRACT.md",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "_build_note": "DO NOT add --sourcemap to tsup. Sourcemaps would expose original TS source, comments, and architecture to anyone who runs `npm install`. See .npmignore.",
    "build": "tsup src/index.ts --format esm,cjs --clean --no-sourcemap && tsc --emitDeclarationOnly --outDir dist",
    "dev": "tsup src/index.ts --format esm,cjs --watch --no-sourcemap",
    "typecheck": "tsc --noEmit",
    "release:prepare": "npm run typecheck --workspace=@dexterai/dextercard && npm run build --workspace=@dexterai/dextercard && npm run typecheck && npm run build && node ./scripts/check-no-sourcemaps.cjs && node ./scripts/check-module-formats.mjs && npm run test:compat",
    "prepublishOnly": "npm run release:prepare",
    "release": "npm publish --access public",
    "test:siwx": "node --test test/access-origin.test.mjs",
    "test:compat": "node --test test/access-origin.test.mjs test/prepared-receipt.test.mjs"
  },
  "keywords": [
    "x402",
    "mcp",
    "dexter",
    "opendexter",
    "tools",
    "registrations"
  ],
  "license": "MIT",
  "dependencies": {
    "@dexterai/dextercard": "^0.5.0",
    "@dexterai/vault": "0.43.4",
    "@dexterai/x402": "6.0.2",
    "@dexterai/x402-core": "1.5.2",
    "@modelcontextprotocol/sdk": "1.30.0",
    "@x402/core": "2.26.0",
    "@x402/extensions": "2.26.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsup": "^8.5.1",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=22"
  },
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Dexter-DAO/opendexter-ide.git",
    "directory": "packages/x402-mcp-tools"
  }
}
